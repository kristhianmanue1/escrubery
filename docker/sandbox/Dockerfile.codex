FROM node:20-alpine
# VERSION = ultima release detectada por el poller (rebuild solo cuando cambia)
ARG VERSION=latest
RUN npm install -g @openai/codex@${VERSION} --no-audit --no-fund --omit=dev
ENTRYPOINT ["codex"]
