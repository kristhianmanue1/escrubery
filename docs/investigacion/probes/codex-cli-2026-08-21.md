# Probe codex-cli — conversaciones (CE-T2, 2026-08-21)

**Ticket:** CE-T2 del plan `docs/investigacion/Plan_Conversation_Event_v0.md` (issue #2).
**Runner:** `npm run probe:conversacion -- --cli codex-cli` (backend). **Modo:** read-only
sobre `~/.codex/sessions/**/rollout-*.jsonl` (626 archivos, un registro JSON por línea).
Salida solo bajo `var/probes/codex-cli/` (gitignored).

## Superficie usada y decisión

- **Superficie:** `almacen_interno` / `rollout-*.jsonl` / estabilidad `interna`.
- Superficies *soportadas* del issue #2 (hooks, App Server, `exec --json`) exigen ejecutar
  codex → excluidas por criterio D1 (artefactos primero). El rollout es además el insumo de
  `resume`, así que razonablemente persistente — pero sin contrato público de estabilidad.

## Hallazgos (254 sesiones únicas; registros julio–agosto 2026)

| Tipo del contrato | Veredicto | Conteo | Método |
|---|---|---:|---|
| `sesion_iniciada` | ok | 254 | `session_meta` deduplicada por `session_id` (los forks re-emiten meta) |
| `prompt_enviado` | ok | 17135 | `event_msg.payload.type = 'user_message'` |
| `turno_finalizado` | ok | 15230 | `event_msg.payload.type = 'task_complete'` (lleva `turn_id` y duración) |
| `turno_fallido` | no_disponible | null | `task_complete` no lleva estado de error; no se infiere |
| `turno_interrumpido` | ok | 604 | `event_msg.payload.type = 'turn_aborted'` (lleva `reason` y `turn_id`) |
| `compactacion` | ok | 2077 | `event_msg.context_compacted` (1030) + registro `compacted` (1047) — doble señal |
| `sesion_cerrada` | no_disponible | null | sin evento de cierre en el rollout |

5 muestras normalizadas validadas contra el schema `conversation-event/v0`.

**Señal destacada:** codex es el CLI con mejor observabilidad de turnos de los cuatro
planeados — `turn_id` estable por turno, interrupciones con `reason` explícito, y el par
task_started/task_complete permite turnos exactos (no pasos como opencode).

## Gate de privacidad (DoD)

Grep de cadenas reales (`introspeccion_omitida`, `jarvis`, `keybindingFlavor`, nombres de
archivo rollout) sobre `var/probes/codex-cli/`: **0 matches**. El walker solo acumula
`type`/`payload.type`/`session_id` — nunca contenido de payload. Los archivos rollout
contienen conversación completa en claro: el probe los lee pero jamás los copia ni proyecta.

## Recomendación para CE-T5

- **Historial:** rollout jsonl es la superficie natural (insumo oficial de `resume`).
- **turno_fallido:** candidato a cubrir por hooks en vivo (pendiente ToS), no por historial.
- Riesgo: el formato rollout es interno; validar por versión en el colector real.
