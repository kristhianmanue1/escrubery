# Probe cline — conversaciones (CE-T3, 2026-08-21)

**Ticket:** CE-T3 del plan `docs/investigacion/Plan_Conversation_Event_v0.md` (issue #2).
**Runner:** `npm run probe:conversacion -- --cli cline` (backend). **Modo:** read-only sobre
`~/.cline/data/` (db/sessions.db sqlite + tasks/*/ui_messages.json y
api_conversation_history.json — de este último SOLO se proyecta el campo `role`).
Salida solo bajo `var/probes/cline/` (gitignored).

## Superficie usada y decisión

- **Superficie:** `almacen_interno` / `sessions.db` + `tasks/*` / estabilidad `interna`.
- Superficies *soportadas* del issue #2 (hooks, `history --json`, SDK/stream) exigen
  ejecutar cline → excluidas por criterio D1 (artefactos primero; ejecución con ToS curado).

## Hallazgos (174 sesiones; 19 tareas con historial)

| Tipo del contrato | Veredicto | Conteo | Método |
|---|---|---:|---|
| `sesion_iniciada` | ok | 174 | tabla `sessions` (fila por sesión con `started_at`) |
| `prompt_enviado` | ok | 223 | `api_conversation_history.json`: `role='user'` (solo role proyectado) |
| `turno_finalizado` | parcial | 15 | `ui_messages say.completion_result` (tarea≈turno en cline); señal secundaria: status terminal de sesión |
| `turno_fallido` | parcial | 8 | `ask.api_req_failed` (5) + `say.error` (3) a nivel tarea; **señal de sesión: 87/174 sesiones terminaron `failed`** |
| `turno_interrumpido` | parcial | 3 | `sessions.status='cancelled'` es cancelación de sesión; interrupción de turno no distinguible |
| `compactacion` | no_disponible | null | sin marcador en ui_messages ni sessions.db |
| `sesion_cerrada` | ok | 168 | `sessions.ended_at` NOT NULL con `exit_code` y `status` — **cierre formal de sesión (único CLI de los 4 que lo tiene)** |

6 muestras normalizadas validadas contra el schema.

**Señales destacadas:**
- cline es el único con ciclo de sesión completo (inicio + cierre con exit_code).
- La tasa de sesiones `failed` (50%) es una señal operativa fuerte para gobernanza
  (motivos en `say.error`/`api_req_failed` — contenido no proyectado aquí).

## Gate de privacidad (DoD)

Grep de cadenas reales (`estas en línea` —prompt real observado en recon—, `deepseek`,
`jarvis`, `introspeccion_omitida`) sobre `var/probes/cline/`: **0 matches**. De
`api_conversation_history.json` solo se cuenta `role`; de `ui_messages.json` solo
`type/say/ask`.

## Recomendación para CE-T5

- **Historial:** sessions.db (metadatos de sesión) + api_conversation_history (roles).
- **turnos exactos:** no observables en artefactos → hooks en vivo (pendiente ToS).
- **compactación:** no existe hoy en los artefactos → el adaptador debe reportar
  `no_disponible`, no sintetizarla.
