# Probe kimi-code — conversaciones (CE-T4, 2026-08-21)

**Ticket:** CE-T4 del plan `docs/investigacion/Plan_Conversation_Event_v0.md` (issue #2).
**Runner:** `npm run probe:conversacion -- --cli kimi-code` (backend). **Modo:** read-only
sobre `~/.kimi/sessions/<sid>/<agente>/wire.jsonl` (protocolo Wire 1.7; CLI 1.47.0).
Salida solo bajo `var/probes/kimi-code/` (gitignored).

## Superficie usada y decisión

- **Superficie:** `almacen_interno` / `wire.jsonl` / estabilidad `interna`.
- Superficies *soportadas* del issue #2 (hooks, export ZIP/MD) exigen ejecutar kimi →
  excluidas por criterio D1. El wire es el registro de eventos del protocolo interno:
  rica semántica de turnos pero sin contrato público.

## Hallazgos (32 directorios de sesión; 30 con mensajes)

| Tipo del contrato | Veredicto | Conteo | Método |
|---|---|---:|---|
| `sesion_iniciada` | ok | 30 | directorio con ≥1 wire.jsonl con mensajes |
| `prompt_enviado` | ok | 1789 | `message.type='TurnBegin'` (payload NO proyectado: `user_input` trae el prompt completo en claro) |
| `turno_finalizado` | ok | 1637 | `message.type='TurnEnd'` (par Begin/End delimita el turno) |
| `turno_fallido` | no_disponible | null | TurnEnd sin estado; Notification no es marcador estable; no se infiere |
| `turno_interrumpido` | ok | 107 | `message.type='StepInterrupted'` (interrupción de paso; aproximación más cercana a turno) |
| `compactacion` | ok | 61 | `CompactionBegin` (61) contrastado con `CompactionEnd` (56) |
| `sesion_cerrada` | no_disponible | null | `state.json.archived` es estado persistente sin timestamp; sin evento de cierre |

5 muestras normalizadas validadas contra el schema.

**Señal destacada:** el wire de kimi es el único con eventos nativos de interrupción
(StepInterrupted) Y compactación (CompactionBegin/End) distinguibles por tipo — la
compactación no requiere heurísticas (vs opencode: 1 part 'compaction' en 815 sesiones).

## Gate de privacidad (DoD)

`TurnBegin.payload.user_input` contiene prompts completos en claro (verificado en recon,
incluye instrucciones internas del Mediador). El walker solo acumula `message.type`.
Grep de cadenas reales de conversación (`DECÁLOGO SOBERANO`, `AGENT INSTRUCTIONS`,
`jarvis`) sobre `var/probes/kimi-code/`: **0 matches**.

## Recomendación para CE-T5

- **Historial y turnos:** wire.jsonl es la superficie más rica por tipo de evento nativo.
- **turno_fallido:** ausente en los 4 CLIs de artefacto → candidato transversal para hooks.
- Riesgo: protocolo interno (1.7) sin contrato de estabilidad.
